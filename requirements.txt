# Pinned deployment environment; this model-v3 update also forces a clean Cloud rebuild.
numpy==2.0.2
protobuf==5.29.6
streamlit==1.63.0
tensorflow-cpu==2.18.1 ; platform_system != "Darwin"
tensorflow==2.18.1 ; platform_system == "Darwin"
